name: Tidy Single Pkg Workflow

permissions:
  contents: read

on:
  push:
    branches:
      - develop
    paths-ignore:
      - 'docs/**'
      - '.github/**'
  pull_request:
    branches: [ develop ]
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

jobs:

  build_single_pkg:
    name: tidy single pkg
    uses: art-daq/.github/.github/workflows/artdaq-tidy-single-pkg.yml@stable
    with:
      against-ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event_name == 'push' && github.event.before || 0 }}
